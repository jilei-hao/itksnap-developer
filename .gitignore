# Build directories
build/
build-debug/
build-leaks/
build-release/
build-v460/
build-greedy/
build-convertmesh/
build-cmrep/

# Dependency sources, builds, and installs (scripts/build-deps.sh)
lib/
*/aqtinstall.log
aqtinstall.log

# macOS
.DS_Store

# Word/Office lock files (created while a document is open)
~$*

# Leak test logs (large, generated)
memory_leak_profiling/asan_logs/

# Build/test transcripts written at the repo root by the scripts above
*.log
config.local.sh